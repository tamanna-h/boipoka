import React, { use } from 'react';

const Book = ({book}) => {
    // const data = use(bookPromise);

    // console.log(data);
    
    console.log(book);
    
    return (
        <div>
            <h3>Book</h3>
        </div>
    );
};

export default Book;